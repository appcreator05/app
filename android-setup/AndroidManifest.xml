<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dsp.reviews">

    <!-- Permissions required for streaming and Start.io Ads -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="DSP Reviews"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <!-- ============================================== -->
        <!-- Start.io Official SDK Configuration          -->
        <!-- App ID: 203877183                              -->
        <!-- ============================================== -->
        <meta-data
            android:name="com.startapp.sdk.APPLICATION_ID"
            android:value="203877183" />

        <!-- Auto show return ads when user comes back to the app -->
        <meta-data
            android:name="com.startapp.sdk.RETURN_ADS_ENABLED"
            android:value="true" />

        <!-- Optional splash screen ad on launch -->
        <meta-data
            android:name="com.startapp.sdk.SPLASH_ENABLED"
            android:value="false" />

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="CineFlix"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
